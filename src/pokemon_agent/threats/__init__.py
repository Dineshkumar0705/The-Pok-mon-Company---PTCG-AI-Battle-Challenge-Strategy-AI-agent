"""Named, replay-audit-derived threat handling — one file per finding.

Each module here traces to a specific real-ladder replay that showed the
heuristic scorer mishandling a card interaction. See each module's docstring
for the finding it came from.
"""
